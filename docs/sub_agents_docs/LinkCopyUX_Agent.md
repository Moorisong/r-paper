# 링크 복사 유도 UX 개선 - 서브 에이전트 문서

## 목적
롤링페이퍼 생성 후 사용자가 링크를 분실하지 않도록 강력한 UX 유도 시스템 구현

---

## 구현 요구사항

### 1. 생성 직후 링크 복사 모달 (`LinkCopyModal`)

**트리거 조건:**
- 롤링페이퍼 생성 후 `paper-view` 페이지로 처음 진입할 때
- URL에 `?new=true` 쿼리 파라미터가 있는 경우에만 표시
- `use-create-paper.js` 훅에서 생성 후 네비게이션 시 해당 파라미터 추가

**모달 UI:**
```
┌─────────────────────────────────┐
│     🎉 롤링페이퍼가 만들어졌어요!    │
│                                 │
│  ⚠️ 이 링크를 꼭 저장하세요!        │
│  링크를 잃어버리면 다시 찾을 수 없어요  │
│                                 │
│  ┌─────────────────────────┐   │
│  │ [현재 URL 표시]          │   │
│  └─────────────────────────┘   │
│                                 │
│     [ 📋 링크 복사하기 ]           │
│                                 │
│        나중에 복사할게요            │
└─────────────────────────────────┘
```

**동작:**
- [링크 복사하기] 클릭: 클립보드 복사 → 모달 닫기 → 성공 토스트
- [나중에 복사할게요] 클릭: 2차 경고 표시

---

### 2. "나중에" 선택 시 2차 경고

**UI:**
```
┌─────────────────────────────────┐
│   정말로 닫으시겠어요?              │
│                                 │
│   링크를 복사하지 않으면             │
│   이 롤링페이퍼를 다시 찾을 수 없어요   │
│   😢                            │
│                                 │
│   [그래도 닫기]   [복사하기]         │
└─────────────────────────────────┘
```

**동작:**
- [그래도 닫기]: 모달 닫기 → 상단 경고 배너 표시
- [복사하기]: 클립보드 복사 → 모달 닫기 → 성공 토스트

---

### 3. 상단 고정 알림 배너 (`LinkWarningBanner`)

**트리거 조건:**
- 링크 복사 없이 모달을 닫은 경우에만 표시
- `showLinkWarningBanner` 상태로 관리

**UI:**
```
┌────────────────────────────────────────────────┐
│ ⚠️ 링크를 저장하세요! 나중에 찾을 수 없어요  [복사하기] ✕ │
└────────────────────────────────────────────────┘
```

**스타일:**
- 화면 상단 고정 (fixed, top-0)
- 경고 색상 (amber/yellow 계열)
- 애니메이션: 위에서 슬라이드 인

**동작:**
- [복사하기] 클릭: 클립보드 복사 → 배너 숨김 → 성공 토스트
- [✕] 클릭: 배너 숨김 (경고 없이)

---

## 수정 대상 파일

### 신규 생성
- `/frontend/src/components/paper/link-copy-modal.jsx` - 링크 복사 모달 컴포넌트
- `/frontend/src/components/paper/link-warning-banner.jsx` - 상단 경고 배너 컴포넌트

### 수정
- `/frontend/src/hooks/use-create-paper.js` - 생성 후 `?new=true` 쿼리 파라미터 추가
- `/frontend/src/pages/paper-view.jsx` - 모달 및 배너 통합
- `/frontend/src/constants/index.js` - 새로운 메시지 상수 추가

---

## 상태 관리

```javascript
// paper-view.jsx에서 관리할 상태
const [isLinkModalOpen, setIsLinkModalOpen] = useState(false);
const [showSecondWarning, setShowSecondWarning] = useState(false);
const [showLinkWarningBanner, setShowLinkWarningBanner] = useState(false);
const [hasCopiedLink, setHasCopiedLink] = useState(false);

// URL 쿼리 파라미터 체크
useEffect(() => {
  const params = new URLSearchParams(window.location.search);
  if (params.get('new') === 'true') {
    setIsLinkModalOpen(true);
    // URL에서 쿼리 파라미터 제거 (히스토리 정리)
    window.history.replaceState({}, '', window.location.pathname);
  }
}, []);
```

---

## 애니메이션 요구사항

- 모달: `framer-motion` 사용, 스케일 + 페이드 인
- 배너: 위에서 슬라이드 다운
- 복사 성공 시: 버튼에 체크마크 애니메이션

---

## 테스트 시나리오

1. 롤링페이퍼 생성 → 모달 표시 확인
2. [링크 복사하기] 클릭 → 복사 성공, 모달 닫힘, 배너 미표시
3. [나중에 복사할게요] 클릭 → 2차 경고 표시
4. [그래도 닫기] 클릭 → 모달 닫힘, 상단 배너 표시
5. 배너 [복사하기] 클릭 → 복사 성공, 배너 숨김
6. 직접 URL 접근 (new 파라미터 없음) → 모달 미표시
